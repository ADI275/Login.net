@page
@using LoginApplication.MyComponents;
@using LoginApplication.ViewModels;
@model LoginApplication.Pages.RecordsModel
@{
}

<component>@(await Html.RenderComponentAsync<Table<Record>>(RenderMode.ServerPrerendered, new { Items = Model.Records, ModelType = "Records" }))</component>
