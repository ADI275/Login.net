@page
@using LoginApplication.MyComponents;
@model LoginApplication.Pages.ReportModel
@{
}

<component>@(await Html.RenderComponentAsync<Table<Report>>(RenderMode.ServerPrerendered, new { Items = Model.Reports, ModelType = "Reports" }))</component>
